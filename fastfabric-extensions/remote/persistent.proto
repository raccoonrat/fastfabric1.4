syntax = "proto3";

import "protos/common/common.proto";

package grpc;

service PersistentPeer {
    rpc Store(common.Block) returns (Result) {    }
    rpc CreateLedger(common.Block) returns (Result) {    }
}

message Result {
}
